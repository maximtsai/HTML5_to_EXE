# Icon Folder

Place your game icon here as `icon.ico`.

The build script will automatically copy it into the Tauri build.

**Requirements:**
- File must be named `icon.ico`
- Recommended size: 256×256 pixels (can contain multiple sizes embedded)
